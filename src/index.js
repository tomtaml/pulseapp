const JSON_HEADERS={"content-type":"application/json; charset=utf-8"};const MAX_BODY_BYTES=20000;const ALLOWED_VARIANTS=new Set(["fi-fleet","fi-citizen","uk-v2h"]);const ALLOWED_GROUPS=new Set(["fleet_driver","dispatcher","fleet_manager","citizen","accessibility_representative","road_user","other"]);const PII_KEYS=new Set(["name","full_name","first_name","last_name","email","phone","telephone","address","street_address","postal_address","postcode","zip","social_security_number"]);
function securityHeaders(response){const h=new Headers(response.headers);h.set("X-Content-Type-Options","nosniff");h.set("X-Frame-Options","DENY");h.set("Referrer-Policy","no-referrer");h.set("Permissions-Policy","camera=(), microphone=(), geolocation=(), payment=(), usb=()");h.set("Cross-Origin-Opener-Policy","same-origin");h.set("Cross-Origin-Resource-Policy","same-origin");h.set("Content-Security-Policy","default-src 'self'; script-src 'self' https://challenges.cloudflare.com; frame-src https://challenges.cloudflare.com; connect-src 'self' https://challenges.cloudflare.com; style-src 'self'; img-src 'self' data:; font-src 'self'; base-uri 'none'; form-action 'self'; frame-ancestors 'none'");return new Response(response.body,{status:response.status,statusText:response.statusText,headers:h})}
function json(data,status=200){return securityHeaders(new Response(JSON.stringify(data),{status,headers:JSON_HEADERS}))}
function sanitize(value,depth=0){if(depth>8)return null;if(Array.isArray(value))return value.slice(0,100).map(v=>sanitize(v,depth+1));if(value&&typeof value==="object"){const out={};for(const[k,v]of Object.entries(value)){if(PII_KEYS.has(String(k).toLowerCase()))continue;out[k]=sanitize(v,depth+1)}return out}if(typeof value==="string")return value.slice(0,1000);if(typeof value==="number")return Number.isFinite(value)?value:null;if(typeof value==="boolean"||value===null)return value;return null}
function integerInRange(v,min,max){const n=Number(v);return Number.isInteger(n)&&n>=min&&n<=max?n:null}function avg(values){const n=values.map(Number).filter(Number.isFinite);return n.length?n.reduce((a,b)=>a+b,0)/n.length:null}function susScore(values){if(!Array.isArray(values)||values.length!==10)return null;const n=values.map(v=>integerInRange(v,1,5));if(n.some(v=>v===null))return null;return n.reduce((s,v,i)=>s+(i%2===0?v-1:5-v),0)*2.5}
async function verifyTurnstile(env,token){if(!env.TURNSTILE_SECRET_KEY)return{success:env.ENVIRONMENT!=="production"};if(!token||typeof token!=="string")return{success:false};const form=new FormData();form.append("secret",env.TURNSTILE_SECRET_KEY);form.append("response",token);form.append("idempotency_key",crypto.randomUUID());const res=await fetch("https://challenges.cloudflare.com/turnstile/v0/siteverify",{method:"POST",body:form});return res.ok?res.json():{success:false}}
function validate(body){if(!body||typeof body!=="object")return"Invalid request.";if(!ALLOWED_VARIANTS.has(body.variant))return"Unknown study variant.";if(!ALLOWED_GROUPS.has(body.participant_group))return"Unknown participant group.";if(!/^[A-Za-z0-9_-]{1,32}$/.test(body.workshop_code||""))return"Invalid workshop code.";if(!["fi","en"].includes(body.language))return"Invalid language.";if(!body.consent_confirmed)return"Research notice/consent acknowledgement is required.";if(!body.prototype_disclaimer_confirmed)return"Prototype disclaimer acknowledgement is required.";return null}
async function handleSubmit(request,env){if(env.COLLECTION_ENABLED!=="true")return json({ok:false,error:"Collection is disabled until ethics/deployment approval."},503);const len=Number(request.headers.get("content-length")||"0");if(len>MAX_BODY_BYTES)return json({ok:false,error:"Submission too large."},413);if(!(request.headers.get("content-type")||"").includes("application/json"))return json({ok:false,error:"JSON required."},415);let body;try{body=await request.json()}catch{return json({ok:false,error:"Malformed JSON."},400)}const problem=validate(body);if(problem)return json({ok:false,error:problem},400);const turnstile=await verifyTurnstile(env,body.turnstile_token);if(!turnstile.success)return json({ok:false,error:"Human-verification failed. Please retry."},403);const clean=sanitize(body);delete clean.turnstile_token;const comprehension=Array.isArray(clean.comprehension_items)?clean.comprehension_items.filter(Boolean).length:null;const sus=susScore(clean.sus_values);const trust=Array.isArray(clean.trust_values)?avg(clean.trust_values):null;const wireless=integerInRange(clean.wireless_acceptance,1,5);const bidi=integerInRange(clean.bidirectional_participation,1,5);const id=crypto.randomUUID();await env.DB.prepare(`INSERT INTO submissions (id,variant,workshop_code,participant_group,language,comprehension_score,sus_completed,sus_score,trust_score,wireless_acceptance,bidirectional_participation,payload_json) VALUES (?,?,?,?,?,?,?,?,?,?,?,?)`).bind(id,clean.variant,clean.workshop_code,clean.participant_group,clean.language,comprehension,sus===null?0:1,sus,trust,wireless,bidi,JSON.stringify(clean)).run();return json({ok:true,submission_id:id})}
export default{async fetch(request,env){const url=new URL(request.url);if(url.pathname==="/api/config"&&request.method==="GET")return json({collection_enabled:env.COLLECTION_ENABLED==="true",turnstile_site_key:env.TURNSTILE_SITE_KEY||null,environment:env.ENVIRONMENT||"unknown"});if(url.pathname==="/api/health"&&request.method==="GET")return json({ok:true});if(url.pathname==="/api/submit"&&request.method==="POST")return handleSubmit(request,env);if(url.pathname.startsWith("/api/"))return json({ok:false,error:"Not found."},404);return securityHeaders(await env.ASSETS.fetch(request))}}
